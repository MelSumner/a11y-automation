<table class="sortable-table min-w-full divide-y divide-gray-200 table-auto">
  <caption class="text-left p-4 sr-only">Potential Violations, their automation status, and WCAG level. Each column header supports sorting.</caption>
  <thead class="bg-gray-50 dark:bg-gray-800">
    <tr>
      <SortableTableHeader
        @field="title"
        @sortOrder={{@sortOrder}}
        @sortBy={{@sortBy}}
        @setSortBy={{this.setSortBy}}
      >
        Failure
      </SortableTableHeader>
      <SortableTableHeader
        @field="linting"
        @sortOrder={{@sortOrder}}
        @sortBy={{@sortBy}}
        @setSortBy={{this.setSortBy}}
      >
        Linting Status
      </SortableTableHeader>
      <SortableTableHeader
        @field="testing"
        @sortOrder={{@sortOrder}}
        @sortBy={{@sortBy}}
        @setSortBy={{this.setSortBy}}
      >
        Testing Status
      </SortableTableHeader>
      <SortableTableHeader
        @field="webAimLevel"
        @sortOrder={{@sortOrder}}
        @sortBy={{@sortBy}}
        @setSortBy={{this.setSortBy}}
      >
        Level
      </SortableTableHeader>
    </tr>
  </thead>
  <tbody class="bg-white dark:bg-gray-600 divide-y divide-gray-200">
    {{#each (sort-by this.sort @model) as |cellitem|}}
      <tr>
        <th scope="row"
          class="whitespace-nowrap text-base text-left font-medium text-gray-900 dark:text-gray-100 p-1"
        >
          <LinkTo
            @route="violations.violation"
            @model={{cellitem.id}}
            class="underline flex px-6 py-4 hover:bg-indigo-50 dark:hover:text-yellow-400 dark:focus:border-yellow-300 dark:hover:bg-transparent dark:hover:border dark:hover:border-yellow-300"
          >
            {{cellitem.title}}
          </LinkTo>
        </th>
        <td class="px-6 py-4 whitespace-nowrap text-base text-gray-500">
          <span class="status status-{{cellitem.linting}}">
            {{cellitem.lintingText}}
          </span>
        </td>
        <td class="px-6 py-4 whitespace-nowrap text-base text-gray-500">
          <span class="status status-{{cellitem.testing}}">
            {{cellitem.testingText}}
          </span>
        </td> 
        <td class="px-6 py-4 whitespace-nowrap text-base text-gray-500">
          <span class="status level-{{cellitem.webAimLevel}}">
            {{cellitem.webAimLevel}}
          </span>
        </td>
      </tr>
    {{/each}}
  </tbody>
</table>
<div id="a11y-notification" class="sr-only" role="region" aria-live="polite"></div>